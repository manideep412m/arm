# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  name: Default

steps:
- task: AzureCLI@2
  inputs:
    azureSubscription: 'aciserviceconnection'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az account set --subscription 94b32def-28a7-475d-bbe4-bfa09615697a'
- task: AzureCLI@2
  inputs:
    azureSubscription: 'aciserviceconnection'
    scriptType: 'bash'
    scriptLocation: 'inlineScript'
    inlineScript: 'az acr create --name armreg3 --resource-group bharathrs --sku SKU --location UKSouth'